{
  "name": "argos-playwright-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Vite + Playwright + Argos CI visual testing proof of concept.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "show-report": "playwright show-report"
  },
  "dependencies": {},
  "devDependencies": {
    "@argos-ci/cli": "^6.1.0",
    "@argos-ci/playwright": "^6.1.0",
    "@playwright/test": "^1.61.1",
    "vite": "^8.1.3"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
